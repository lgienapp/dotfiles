# Dotfiles

Fully automated setup of new Macs:

```
sudo -v && \
curl -fsSL https://raw.githubusercontent.com/lgienapp/dotfiles/main/defaults.sh | bash && \
curl -fsSL https://raw.githubusercontent.com/lgienapp/dotfiles/main/setup.sh | bash
```
